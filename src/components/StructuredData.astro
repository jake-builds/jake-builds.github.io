---
export interface Props {
  type: "BlogPosting" | "Person" | "WebSite";
  data: any;
}

const { type, data } = Astro.props;

let structuredData = {};

if (type === "BlogPosting") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    headline: data.title,
    description: data.description,
    author: {
      "@type": "Person",
      name: data.author || "Jake",
      url: "https://jake-builds.github.io/about",
    },
    datePublished: data.pubDatetime.toISOString(),
    dateModified: data.modDatetime?.toISOString() || data.pubDatetime.toISOString(),
    publisher: {
      "@type": "Person",
      name: "Jake",
      logo: {
        "@type": "ImageObject",
        url: "https://jake-builds.github.io/jake-avatar.jpg",
      },
    },
    mainEntityOfPage: {
      "@type": "WebPage",
      "@id": data.url,
    },
    image: data.ogImage || "https://jake-builds.github.io/jake-avatar.jpg",
    articleSection: data.tags?.[0] || "Technology",
    keywords: data.tags?.join(", ") || "",
    wordCount: data.wordCount,
    timeRequired: data.readingTime,
  };
} else if (type === "Person") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "Person",
    name: "Jake Builds",
    url: "https://jake-builds.github.io",
    image: "https://jake-builds.github.io/jake-avatar.jpg",
    sameAs: [
      "https://github.com/jake-builds",
      "https://x.com/jakexshi",
    ],
    jobTitle: "Software Engineer",
    description:
      "Jake's personal blog and coding experiments.",
  };
} else if (type === "WebSite") {
  structuredData = {
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: "Jake Builds",
    url: "https://jake-builds.github.io",
    description:
      "Jake's personal blog and coding experiments.",
    author: {
      "@type": "Person",
      name: "Jake Builds",
    },
    potentialAction: {
      "@type": "SearchAction",
      target: "https://jake-builds.github.io/search?q={search_term_string}",
      "query-input": "required name=search_term_string",
    },
  };
}
---

<script type="application/ld+json" set:html={JSON.stringify(structuredData)} />